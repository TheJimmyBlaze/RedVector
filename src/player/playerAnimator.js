import {
    useSpriteSheet,
    useSpriteSheetRun
} from 'titanium';

export const usePlayerAnimator = ({
    playerStateMachine
}) => {

    const update = () => {

    };

    const draw = () => {

    };

    return {
        actions: {
            update,
            draw
        }
    };
};